import 'package:flutter/material.dart';

class TrolleyPairingPage extends StatelessWidget {
  const TrolleyPairingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Trolley Pairing')),
      body: const Center(
        child: Text('Trolley Pairing Coming Soon'),
      ),
    );
  }
}